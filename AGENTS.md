# StorageUnit SimLay — Continuity Gate

This repository is the canonical StorageUnit SimLay code lineage: `ifalsetto/StorageUnit_SimLay`.

Before creating a new SimLay app, dashboard, inventory store, valuation engine, API, or deployment:

1. Query the FalseTech Continuity Engine project registry and decision memory for `StorageUnit`, `SimLay`, `inventory`, `valuation`, `owner`, and semantic equivalents.
2. Inspect this repository and relevant branches before scaffolding a parallel implementation.
3. Treat older HTML dashboards, Streamlit builds, ZIP exports, Drive folders, and `legacy-streamlit-backup` as feature/reference sources unless continuity evidence promotes them.
4. Merge superior behavior forward into this repository rather than creating another runtime.
5. Never infer inventory ownership. Valid owners are `Thomas`, `Mine`, and `Unassigned`.
6. Never merge physical-item records solely because names/photos look similar. Flag suspected duplicates for review.
7. Preserve confidence/source truthfulness. `Verified`, `Inferred`, and `Unknown` are evidence states.
8. Never commit private inventory databases, uploads, or exports to the public repository.
9. Destructive inventory actions must be auditable and recoverable where practical.
10. At the end of a substantial merge, update continuity documentation and the external Continuity Engine through its established write path when available.

Current continuity master version: `2.0.0-continuity-master`.
